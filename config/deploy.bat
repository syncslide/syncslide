ssh arch@clippycat.ca "set -eo pipefail; cd syncSlide && git pull origin main --rebase && cd syncslide-websocket && cargo build --release && cd ~ && sudo cp syncSlide/config/syncSlide.service /etc/systemd/system && sudo systemctl daemon-reload && sudo cp syncSlide/config/syncSlide.conf /etc/caddy/conf.d && sudo chown root:root /etc/caddy/conf.d/syncSlide.conf && sudo systemctl reload caddy && sudo systemctl restart syncSlide"
